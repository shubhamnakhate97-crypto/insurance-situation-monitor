{
  "name": "insight-underwriting-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "verify": "pnpm test && pnpm build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "pdf-lib": "^1.17.1"
  }
}
